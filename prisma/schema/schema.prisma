generator client {
  provider = "prisma-client"
  output   = "../../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
}

enum Role {
  STUDENT
  TUTOR
  ADMIN
}

enum Gender {
  MALE
  FEMALE
}

enum BookingStatus {
  CONFIRMED
  COMPLETED
  CANCELLED
}
